---
title: "SoulForge"
description: "Graph-powered code intelligence — multi-agent coding with codebase-aware AI."
icon: "terminal"
---

<img src="/images/main-1.png" alt="SoulForge" />
<img src="/images/main-2.png" alt="SoulForge" />

Your real Neovim -- config, plugins, LSP -- embedded in an AI agent that understands your codebase structurally. Graph-powered intelligence, multi-agent dispatch, 9 providers. Works over SSH.

## How it compares

| | SoulForge | Claude Code | Copilot CLI | Codex CLI | Aider |
|---|---|---|---|---|---|
| **Editor** | Embedded Neovim (your config) | No editor | No editor | No editor | No editor |
| **Code graph** | SQLite graph -- PageRank, blast radius, cochange, clone detection, FTS5 | None (file reads + grep) | None | None (MCP plugins) | Tree-sitter repo map + PageRank |
| **Code intelligence** | 4-tier fallback: LSP, ts-morph, tree-sitter, regex. Dual LSP. 12 operations. 33+ languages | LSP via plugin marketplace (rename via cclsp, no auto-install) | No agent LSP access | MCP-based LSP via plugins | Tree-sitter AST |
| **Compound tools** | `rename_symbol`, `move_symbol`, `refactor` -- compiler-guaranteed, atomic | Rename via LSP | -- | -- | -- |
| **Semantic context** | AST summaries for top 500 symbols, dispatch auto-enrichment | -- | -- | -- | Tree-sitter tag summaries |
| **Multi-agent** | Parallel dispatch (8 agents, 3 concurrent, shared cache, edit ownership) | Subagents + Agent Teams | Subagents + Fleet | Multi-agent v2 + subagents | Single agent |
| **Providers** | 9 (LLM Gateway, Anthropic, OpenAI, Google, xAI, Ollama, +3) + custom | Anthropic (API, Bedrock, Vertex) | Multi-model | OpenAI models | 100+ LLMs |
| **Task routing** | Per-task model assignment (plan, code, explore, search, trivial, cleanup, compact) | Single model | Single model | Per-agent model | Single model |
| **Context management** | 2-layer: per-step pruning + V1/V2 compaction | Auto-compaction | Context window management | Server-side compaction | -- |
| **License** | BSL 1.1 (source-available) | Proprietary | Proprietary | Apache 2.0 | Apache 2.0 |

<Note>Competitor capabilities verified as of March 29, 2026.</Note>

## Highlights

<CardGroup cols={2}>

<Card title="Embedded Neovim" icon="code">
  Your Neovim with LazyVim, LSP via Mason, treesitter highlighting. The AI edits through the same editor you use.
</Card>

<Card title="Multi-agent dispatch" icon="users">
  Parallel explore, code, and web search agents with shared file cache and edit coordination.
</Card>

<Card title="Graph-powered repo map" icon="diagram-project">
  SQLite codebase graph with PageRank, cochange analysis, blast radius, and clone detection.
</Card>

<Card title="4-tier code intelligence" icon="brain">
  LSP, ts-morph, tree-sitter, regex fallback chain across 33+ languages with dual LSP backend.
</Card>

<Card title="Compound tools" icon="wrench">
  `rename_symbol`, `move_symbol`, `refactor`, `project` do the complete job in one call.
</Card>

<Card title="Task router" icon="route">
  Assign models per task: Opus for planning, Sonnet for coding, Haiku for search.
</Card>

<Card title="Context management" icon="layers">
  Rolling tool-result pruning per step, plus V1/V2 compaction on threshold.
</Card>

<Card title="User steering" icon="compass">
  Type messages while the agent works -- they queue and inject into the next step.
</Card>

<Card title="Floating terminals" icon="terminal">
  Spawn, resize, and manage terminal sessions alongside the chat. Powered by Ghostty's terminal emulator.
</Card>

<Card title="24 themes" icon="palette">
  Catppuccin, Dracula, Gruvbox, Nord, Tokyo Night, Rose Pine, and more. Create custom themes with hot reload.
</Card>

<Card title="9 providers" icon="plug">
  LLM Gateway, Anthropic, OpenAI, Google, xAI, Ollama, OpenRouter, Vercel AI Gateway, Proxy, and custom OpenAI-compatible APIs.
</Card>

<Card title="Per-model cost tracking" icon="chart-line">
  Cache-aware pricing across 30+ models. Per-task, per-agent, per-model breakdown with real-time display.
</Card>

<Card title="Cross-tab coordination" icon="table-columns">
  Up to 5 concurrent tabs with per-tab model selection, advisory file claims, and git blocking during dispatch.
</Card>

<Card title="Project toolchain" icon="toolbox">
  Auto-detects lint, typecheck, test, and build across 23 ecosystems with pre-commit enforcement.
</Card>

<Card title="Skills and approval gates" icon="shield-check">
  Installable skills for domain-specific capabilities. Destructive actions individually prompted.
</Card>

</CardGroup>

## Tool suite

SoulForge ships 33 tools organized by capability:

### Code intelligence

| Tool | What it does |
|------|-------------|
| `read_code` | Extract function/class/type by name (LSP-powered) |
| `navigate` | Definition, references, call hierarchy, implementations |
| `analyze` | File diagnostics, unused symbols, complexity |
| `rename_symbol` | Compiler-guaranteed rename across all files |
| `move_symbol` | Move to another file + update all importers |
| `refactor` | Extract function/variable, organize imports |

### Codebase analysis (zero LLM cost)

| Tool | What it does |
|------|-------------|
| `soul_grep` | Count-mode ripgrep with repo map intercept |
| `soul_find` | Fuzzy file/symbol search, PageRank-ranked, signatures included |
| `soul_analyze` | Identifier frequency, unused exports, file profiles, top files, packages, symbols by kind |
| `soul_impact` | Dependency graph -- dependents, cochanges, blast radius |

### Project management

| Tool | What it does |
|------|-------------|
| `project` | Auto-detected lint, format, test, build, typecheck across 23 ecosystems |
| `project(list)` | Discover monorepo packages with per-package capabilities |
| `dispatch` | Parallel multi-agent execution (up to 8 agents, 3 concurrent) |
| `git` | Structured git operations with auto co-author tracking |

## LLM providers

| Provider | Models | Setup |
|----------|--------|-------|
| **LLM Gateway** | Multi-model gateway (OpenAI, Claude, Gemini, DeepSeek) | `LLM_GATEWAY_API_KEY` |
| **Anthropic** | Claude Opus 4.6, Sonnet 4, Haiku 3.5 | `ANTHROPIC_API_KEY` |
| **OpenAI** | GPT-4.5, o3, o4-mini | `OPENAI_API_KEY` |
| **Google** | Gemini 2.5 Pro/Flash | `GOOGLE_GENERATIVE_AI_API_KEY` |
| **xAI** | Grok 3 | `XAI_API_KEY` |
| **Ollama** | Any local model | Auto-detected |
| **OpenRouter** | 200+ models | `OPENROUTER_API_KEY` |
| **Vercel AI Gateway** | Unified gateway for 15+ providers | `AI_GATEWAY_API_KEY` |
| **Proxy** | Local proxy with auto-lifecycle | `PROXY_API_KEY` |
| **Custom** | Any OpenAI-compatible API | Any env var |

<Note>
Read the [full story](https://proxysoul.com/blog/soulforge) on why we built SoulForge.
</Note>
